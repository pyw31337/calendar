diff --git a/assets/app.css b/assets/app.css
index 7b1d03e..228ece2 100644
--- a/assets/app.css
+++ b/assets/app.css
@@ -3902,3 +3902,17 @@ textarea.form-input {
   .manual-mobile-friendly article h3 { font-size: 0.95rem !important; }
   .manual-mobile-friendly article p { font-size: 0.82rem !important; margin-top: 4px !important; }
 }
+
+
+/* Emoji sheet: never dim/blur the page (composer must stay readable) */
+.bottom-sheet-overlay.emoji-sheet-overlay,
+:root[data-theme="dark"] .bottom-sheet-overlay.emoji-sheet-overlay {
+  background: transparent !important;
+  background-color: transparent !important;
+  -webkit-backdrop-filter: none !important;
+  backdrop-filter: none !important;
+  pointer-events: auto;
+}
+.bottom-sheet-overlay.emoji-sheet-overlay .emoji-sheet {
+  pointer-events: auto;
+}
diff --git a/public/assets/app.css b/public/assets/app.css
index 7b1d03e..228ece2 100644
--- a/public/assets/app.css
+++ b/public/assets/app.css
@@ -3902,3 +3902,17 @@ textarea.form-input {
   .manual-mobile-friendly article h3 { font-size: 0.95rem !important; }
   .manual-mobile-friendly article p { font-size: 0.82rem !important; margin-top: 4px !important; }
 }
+
+
+/* Emoji sheet: never dim/blur the page (composer must stay readable) */
+.bottom-sheet-overlay.emoji-sheet-overlay,
+:root[data-theme="dark"] .bottom-sheet-overlay.emoji-sheet-overlay {
+  background: transparent !important;
+  background-color: transparent !important;
+  -webkit-backdrop-filter: none !important;
+  backdrop-filter: none !important;
+  pointer-events: auto;
+}
+.bottom-sheet-overlay.emoji-sheet-overlay .emoji-sheet {
+  pointer-events: auto;
+}
diff --git a/src/ui/ui-calendar-core.js b/src/ui/ui-calendar-core.js
index 1782800..f4e8e4e 100644
--- a/src/ui/ui-calendar-core.js
+++ b/src/ui/ui-calendar-core.js
@@ -1667,6 +1667,24 @@ export function CommentsSection({
       )
     );
   }),
+  /* Full Width "이전 채팅 더보기" Button at bottom of list */
+  (messagesToShow.length > 0 || (totalChatCountProp || 0) > 0) && typeof onMore === 'function' && /*#__PURE__*/React.createElement("button", {
+    type: "button",
+    onClick: onMore,
+    style: {
+      width: '100%',
+      backgroundColor: 'color-mix(in srgb, var(--bg-primary) 96%, black)',
+      border: 'none',
+      borderRadius: 'var(--radius-md)',
+      padding: '8px 0',
+      marginTop: '4px',
+      fontSize: '0.85rem',
+      fontWeight: 'bold',
+      color: 'var(--text-main)',
+      cursor: 'pointer',
+      textAlign: 'center'
+    }
+  }, "이전 채팅 더보기")
   )),
   /* Input row */
   /*#__PURE__*/React.createElement("div", {
diff --git a/src/ui/ui-overlays.js b/src/ui/ui-overlays.js
index c83adff..39dee6e 100644
--- a/src/ui/ui-overlays.js
+++ b/src/ui/ui-overlays.js
@@ -831,11 +831,18 @@ export function EmojiPickerSheet({ onSelect, onClose }) {
   );
   const sheet = /*#__PURE__*/React.createElement('div', {
     className: 'bottom-sheet-overlay emoji-sheet-overlay',
-    onClick: onClose
+    onClick: onClose,
+    style: {
+      background: 'transparent',
+      backgroundColor: 'transparent',
+      backdropFilter: 'none',
+      WebkitBackdropFilter: 'none',
+      // keep sheet at bottom; do not dim the page so composer stays visible
+    }
   }, /*#__PURE__*/React.createElement('div', {
     className: 'bottom-sheet emoji-sheet',
     onClick: e => e.stopPropagation(),
-    style: { maxHeight: '60vh' }
+    style: { maxHeight: '60vh', boxShadow: '0 -8px 30px rgba(0,0,0,0.18)' }
   },
     /*#__PURE__*/React.createElement('div', { className: 'bottom-sheet-header' },
       /*#__PURE__*/React.createElement('h4', null, '이모티콘'),
