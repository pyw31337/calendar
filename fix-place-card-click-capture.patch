diff --git a/assets/app.css b/assets/app.css
index 909c0c6..4eae7d2 100644
--- a/assets/app.css
+++ b/assets/app.css
@@ -358,8 +358,7 @@ body:has(.admin-login-gate) {
 .calendar-card,
 .summary-card,
 .poll-card,
-.memo-card-hover,
-.place-card-row {
+.memo-card-hover {
   animation: motion-fade-up var(--motion-slow) var(--motion-ease-out) both;
   will-change: transform, opacity;
 }
@@ -456,7 +455,6 @@ body:has(.admin-login-gate) {
 .section-toggle-btn,
 .main-menu-item,
 .memo-card-hover,
-.place-card-row,
 .confirmed-meeting,
 .color-swatch-trigger,
 .admin-calendar-picker-btn,
@@ -502,7 +500,6 @@ body:has(.admin-login-gate) {
 .poll-voter-option,
 .main-menu-item,
 .memo-card-hover,
-.place-card-row,
 .confirmed-meeting,
 .color-swatch-trigger,
 .admin-calendar-picker-btn,
@@ -4309,7 +4306,6 @@ textarea.form-input {
 
 
 /* Place list cards: no press/scale — they are rows, not buttons */
-.place-card-row,
 .place-card-row:active,
 .place-card-row.is-pressing,
 .place-card-row:hover {
@@ -4319,3 +4315,37 @@ textarea.form-input {
   position: relative;
   z-index: 3;
 }
+
+/* Place list cards are rows, not pressable buttons */
+.place-card-row,
+.place-card-row:hover,
+.place-card-row:active,
+.place-card-row.is-pressing,
+.place-card-row:focus,
+.place-card-row:focus-visible {
+  transform: none !important;
+  filter: none !important;
+  opacity: 1 !important;
+  will-change: auto !important;
+  isolation: auto !important;
+  overflow: visible !important;
+}
+.place-card-actions {
+  position: absolute !important;
+  top: 6px !important;
+  right: 6px !important;
+  z-index: 20 !important;
+  pointer-events: auto !important;
+}
+.place-card-action-btn {
+  position: relative !important;
+  z-index: 21 !important;
+  pointer-events: auto !important;
+  width: 36px !important;
+  height: 36px !important;
+  transform: none !important;
+}
+.place-card-action-btn:active,
+.place-card-action-btn.is-pressing {
+  transform: none !important;
+}
diff --git a/public/assets/app.css b/public/assets/app.css
index 909c0c6..4eae7d2 100644
--- a/public/assets/app.css
+++ b/public/assets/app.css
@@ -358,8 +358,7 @@ body:has(.admin-login-gate) {
 .calendar-card,
 .summary-card,
 .poll-card,
-.memo-card-hover,
-.place-card-row {
+.memo-card-hover {
   animation: motion-fade-up var(--motion-slow) var(--motion-ease-out) both;
   will-change: transform, opacity;
 }
@@ -456,7 +455,6 @@ body:has(.admin-login-gate) {
 .section-toggle-btn,
 .main-menu-item,
 .memo-card-hover,
-.place-card-row,
 .confirmed-meeting,
 .color-swatch-trigger,
 .admin-calendar-picker-btn,
@@ -502,7 +500,6 @@ body:has(.admin-login-gate) {
 .poll-voter-option,
 .main-menu-item,
 .memo-card-hover,
-.place-card-row,
 .confirmed-meeting,
 .color-swatch-trigger,
 .admin-calendar-picker-btn,
@@ -4309,7 +4306,6 @@ textarea.form-input {
 
 
 /* Place list cards: no press/scale — they are rows, not buttons */
-.place-card-row,
 .place-card-row:active,
 .place-card-row.is-pressing,
 .place-card-row:hover {
@@ -4319,3 +4315,37 @@ textarea.form-input {
   position: relative;
   z-index: 3;
 }
+
+/* Place list cards are rows, not pressable buttons */
+.place-card-row,
+.place-card-row:hover,
+.place-card-row:active,
+.place-card-row.is-pressing,
+.place-card-row:focus,
+.place-card-row:focus-visible {
+  transform: none !important;
+  filter: none !important;
+  opacity: 1 !important;
+  will-change: auto !important;
+  isolation: auto !important;
+  overflow: visible !important;
+}
+.place-card-actions {
+  position: absolute !important;
+  top: 6px !important;
+  right: 6px !important;
+  z-index: 20 !important;
+  pointer-events: auto !important;
+}
+.place-card-action-btn {
+  position: relative !important;
+  z-index: 21 !important;
+  pointer-events: auto !important;
+  width: 36px !important;
+  height: 36px !important;
+  transform: none !important;
+}
+.place-card-action-btn:active,
+.place-card-action-btn.is-pressing {
+  transform: none !important;
+}
diff --git a/src/ui/ui-places.js b/src/ui/ui-places.js
index 292bc60..d3b2d31 100644
--- a/src/ui/ui-places.js
+++ b/src/ui/ui-places.js
@@ -1849,14 +1849,20 @@ const getPlaceSortDateKey = __deps.getPlaceSortDateKey;
               backgroundColor: 'var(--bg-card)',
               transition: 'border-color 0.15s ease, background-color 0.15s ease'
             },
-            onClick: () => handleSelectPlaceOnMap(place)
+            onClick: event => {
+              const target = event.target;
+              if (target && target.closest && target.closest('button, a, input, textarea, label, .place-card-actions, .place-card-action-btn')) return;
+              handleSelectPlaceOnMap(place);
+            }
           },
             /* Top-right absolute action buttons */
             /*#__PURE__*/React.createElement("div", {
               className: "place-card-actions",
-              style: { position: 'absolute', top: '8px', right: '8px', display: 'flex', alignItems: 'center', gap: '4px', zIndex: 4 },
-              onClick: event => event.stopPropagation(),
-              onPointerDown: event => event.stopPropagation()
+              style: { position: 'absolute', top: '6px', right: '6px', display: 'flex', alignItems: 'center', gap: '2px', zIndex: 20, pointerEvents: 'auto' },
+              onClick: event => { event.preventDefault(); event.stopPropagation(); },
+              onPointerDown: event => { event.stopPropagation(); },
+              onMouseDown: event => { event.stopPropagation(); },
+              onTouchStart: event => { event.stopPropagation(); }
             },
               /*#__PURE__*/React.createElement("button", {
                 type: "button",
